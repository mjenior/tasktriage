# Anthropic API Key (required)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-api-key-here

# Input directories (at least one required)
# You can configure multiple input sources - TaskTriage will check all of them

# USB/Mounted device directory
# Path to mounted note-taking device directory containing daily/ and weekly/ subdirectories
USB_INPUT_DIR=/path/to/your/usb/notes/directory

# Local hard drive directory (optional)
# Additional local directory to check for notes files
LOCAL_INPUT_DIR=/path/to/your/local/notes/directory

# Google Drive Information (optional)
GOOGLE_CREDENTIALS_PATH=/path/to/your/credentials.json
GOOGLE_DRIVE_FOLDER_ID=foo

# Output directories
LOCAL_OUTPUT_DIR=/path/to/your/output/local/directory

# LangSmith tracing (Optional)
LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=TaskTriage
